extends layout

block vars
  - var description = 'Welcome to my page'
  - var additionalCssFiles = []
  - var additionalJsFiles = ['https://ajax.googleapis.com/ajax/libs/angularjs/1.5.0-rc.0/angular.min.js', 'https://cdnjs.cloudflare.com/ajax/libs/angular.js/1.5.0-rc.0/angular-animate.min.js', '/controllers/articlesTagsController.js']

block content  
    h1= title
    
    div(ng-controller="ArticlesTagsCtrl")
        ul.list-standard
            li.list-standard__item.padding-1(ng-repeat="post in posts")
                h2
                    a(href="{{post.url}}", target="_blank") {{post.title}}
                p {{post.meta}}
                ul.tags-list
                    li.tag Tags:
                    li.tag(ng-repeat="tag in post.tags")
                        a(href="/articles/tags/{{tag}}")
                            {{tag}}
                    
